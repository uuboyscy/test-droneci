kind: pipeline
type: docker      
name: clone 

steps:
  - name: echo 
    image: uuboyscy/python-rbmq                  
    commands:  
    - echo "7852f3"
    - pwd
    - hostname -I
    - ls -lhrt
    when: 
      branch:
      - main


# kind: pipeline
# type: exec
# name: default

# platform:
#   os: linux
#   arch: amd64

# steps:
# - name: greeting
#   commands:
#   - echo hello world
#   - hostname -I